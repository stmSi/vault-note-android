{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "The main VaultNote window uses application commands and private reminder notifications.",
  "windows": ["main"],
  "permissions": ["notification:default"]
}
